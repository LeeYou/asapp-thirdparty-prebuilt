name: grpc
version: "1.67.1"
kind: compiled
license: Apache-2.0
os: linux
arch: x64
linkage: static
config: release
components:
  - protobuf (bundled 27.2)
  - protoc
  - grpc_cpp_plugin
toolchain:
  generator: ninja
  preferred: clang
  exception: "Ninja + linux-clang.cmake; SSL via slice OpenSSL package"
  abi: system
