name: openssl
version: "3.5.6"
kind: compiled
license: Apache-2.0
os: linux
arch: x64
linkage: shared
config: debug
toolchain:
  generator: make
  preferred: clang
  exception: "perl Configure + make (OpenSSL official Unix path)"
  abi: system
